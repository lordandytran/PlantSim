// This is to wait for all of the HTML elements to load before trying to manipulate them
$(document).ready(function() {
  // This will allow each div to fold under a header like an accordian
  $('#Plant-segments').accordion();
});
